set -g mouse on
# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Use vim keys to navigate panes
bind -n C-h select-pane -L # -L (left)
bind -n C-l select-pane -R # -R (right)
bind -n C-j select-pane -D # -D (down)
bind -n C-k select-pane -U # -U (up)
